# UniCore 使用指南

## 🚀 快速开始

### 1. 克隆项目

```bash
git clone https://github.com/lolict/Unicore.git
cd Unicore
```

### 2. 使用 Python 虚拟机（推荐，无需安装 Zig）

```bash
# 演示模式
python3 unicore_vm.py

# 交互模式
python3 unicore_vm.py -i
```

### 3. 使用 Zig 虚拟机（需要安装 Zig）

```bash
# 安装 Zig
# Linux: sudo apt install zig
# macOS: brew install zig
# Windows: winget install zig

# 构建并运行
zig build run
```

---

## 📖 UniCore 汇编语言

### 基本指令

```asm
MOVI R1, 10     # 将立即数 10 加载到寄存器 R1
MOV R2, R1      # 将 R1 的值复制到 R2
ADD R3, R1, R2  # R3 = R1 + R2
SUB R4, R1, R2  # R4 = R1 - R2
MUL R5, R1, R2  # R5 = R1 * R2
DIV R6, R5, R2  # R6 = R5 / R2
```

### 逻辑指令

```asm
AND R7, R1, R2  # R7 = R1 & R2
OR R8, R1, R2   # R8 = R1 | R2
XOR R9, R1, R2  # R9 = R1 ^ R2
```

### 内存访问

```asm
LOAD R1, [R2]   # 从内存地址 R2 加载数据到 R1
STORE R1, [R2]  # 将 R1 的值存储到内存地址 R2
```

### 控制流

```asm
JMP label        # 无条件跳转
JE label         # 如果相等则跳转
JNE label        # 如果不相等则跳转
CALL func       # 调用函数
RET             # 返回
```

### 完整示例

```asm
; 计算 1+2+3+...+10
MOVI R1, 1       ; 计数器 = 1
MOVI R2, 0       ; 求和 = 0
MOVI R3, 10      ; 上限 = 10

loop:
ADD R2, R2, R1   ; 求和 += 计数器
ADDI R1, R1, 1  ; 计数器++
CMP R1, R3       ; 比较计数器与上限
JLE loop         ; 如果计数器 <= 上限，跳转回 loop

HALT             ; 结束
```

---

## 🔧 云端同步功能

### 配置 GitHub Gist

```python
import os
from src.cloud.gist.gist_sync import GitHubGistSync

# 设置 Token
os.environ['GITHUB_TOKEN'] = 'ghp_xxxx'

# 初始化
sync = GitHubGistSync(token='your_token', username='your_name')

# 同步数据
gist_id = sync.sync_data('config', {'setting': 'value'})

# 获取数据
data = sync.get_latest_by_type('config')
```

### 配置坚果云 WebDAV

```python
from src.cloud.webdav.webdav_sync import NutstoreSync

# 初始化
sync = NutstoreSync(username='your@email.com', password='password')

# 上传数据
sync.upload_data('config', 'settings.json', {'key': 'value'})

# 下载数据
data = sync.download_data('config', 'settings.json')
```

---

## 🌐 二进制翻译器

### 支持的架构

- ✅ x86/x64
- ✅ ARM (32位)
- ✅ RISC-V
- ✅ MIPS

### 使用示例

```python
from src.core.translator import BinaryTranslator

trans = BinaryTranslator()

# x86 翻译
x86_code = bytes([0x90, 0x01, 0x29, 0xC3])  # NOP, ADD, SUB, RET
program = trans.translateX86(x86_code)

# ARM 翻译
arm_code = bytes([0x00, 0x00, 0x20, 0xE3, 0x00, 0x00, 0x80, 0xE0])
program = trans.translateARM(arm_code)

# 自动检测
program = trans.translateAuto(binary_data)
```

---

## 📱 跨平台支持

### WebAssembly

```bash
cd src/platform/web
# 使用 Emscripten 编译
emcc -o wasm_output.js input.c -s WASM=1
```

### Android

```bash
cd src/platform/android
npm install
npm run build
```

### ESP32

```bash
cd src/platform/esp32
idf.py build
idf.py flash
```

---

## 🛠️ 开发

### 项目结构

```
Unicore/
├── src/
│   ├── core/          # 核心模块
│   │   ├── uniisa.zig
│   │   └── translator.zig
│   ├── cloud/        # 云端同步
│   │   ├── gist/
│   │   ├── webdav/
│   │   ├── p2p/
│   │   └── sync/
│   └── platform/     # 跨平台
├── tests/            # 测试用例
├── unicore_vm.py    # Python 虚拟机
└── README.md
```

### 运行测试

```bash
# Python 测试
python3 unicore_vm.py --demo

# Zig 测试
zig build test
```

---

## ❓ 常见问题

### Q: Python 虚拟机和 Zig 虚拟机有什么区别？

**A:** 
- **Python 虚拟机**: 无需安装任何依赖，直接运行，适合学习和演示
- **Zig 虚拟机**: 性能更好，适合生产环境

### Q: 如何贡献代码？

**A:**
1. Fork 项目
2. 创建新分支
3. 提交更改
4. 推送到分支
5. 创建 Pull Request

---

## 📄 许可证

MIT License

---

## 🙏 致谢

- UniCore Team
- 智谱AI (提供 API 支持)
- GitHub (提供仓库托管)
